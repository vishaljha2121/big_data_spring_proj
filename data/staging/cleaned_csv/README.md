# Staging Cleaned CSV

The teammate-provided `cleaned_data/` CSV.GZ files are treated as staging input only. They are preserved in place and converted into validated Parquet outputs under `data/cleaned/` and `data/curated/`.
